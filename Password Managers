pwd = input("What is the master password?")
tt = "True"
while(tt):
    mode = input("Would you like to add new password or view exsisting one (view, add) if you want to quik choose q")
    if mode == "q":
        tt = "False"
    if mode == "view":
        pass 
    elif mode == "add":
        pass
    else:
        print("Invalid mode.")
        continue
        
